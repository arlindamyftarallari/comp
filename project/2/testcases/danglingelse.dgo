package main;

var (n int;)

func danglingelse(n int) {
	if 1 == 1 {
		n = 1
		if (2 == 3) {
			n = 2;
		} else { // o else tem que vir na mesma linha que o RBRACE, ou então vai haver 
			//um SEMICOLON entre os 2, e a linguagem não aceita isso
			n = 3
		};
	}
};